# Build helper

Run this script to build both native and web game outputs used by the editor embedding step.

Requirements:
- `cmake` (with presets support)
- `emcmake` / Emscripten SDK (optional, required for web builds)

Usage:

```bash
./tools/build/build-all-games.sh
```
